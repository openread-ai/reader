Start Openread local development for this request:

{{request}}

Follow `/skill:openread-local-dev`.

First, confirm or infer:

- Activity ID / Notion state, if any
- platforms in scope: web, macos, windows, ios, android
- UI vs non-UI
- done-when per platform

Then use `flow_agents` in parallel mode for read-only platform scouts before editing. Consolidate findings and propose the smallest safe implementation plan with commit boundaries.
